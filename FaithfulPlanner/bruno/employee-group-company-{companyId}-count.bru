meta {
  name: /employee-group/company/{companyId}/count
  type: http
  seq: 7
}

get {
  url: http://localhost:8080/api/employee-group/company/{{companyId}}/count
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
  timeout: 0
}

meta {
  name: /schedule Schedule User
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/schedule
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
      "operationDayId": 1,
      "userProfileId": 1
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

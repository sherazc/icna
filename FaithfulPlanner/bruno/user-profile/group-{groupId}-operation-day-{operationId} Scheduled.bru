meta {
  name: /group/{groupId}/operation-day/{operationId} Scheduled
  type: http
  seq: 6
}

get {
  url: {{base_url}}/api/company/1/user-profile/group/1/operation-day/1?scheduled=true
  body: json
  auth: bearer
}

params:query {
  scheduled: true
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
  timeout: 0
}

meta {
  name: /group/{groupId}/operation-day/{operationId} Not Scheduled
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/company/1/user-profile/group/1/operation-day/1?scheduled=false
  body: json
  auth: bearer
}

params:query {
  scheduled: false
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
  timeout: 0
}

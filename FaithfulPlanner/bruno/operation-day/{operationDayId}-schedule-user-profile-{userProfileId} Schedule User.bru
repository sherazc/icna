meta {
  name: /{operationDayId}/schedule/user-profile/{userProfileId} Schedule User
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/company/1/operation-day/1/schedule/user-profile/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
  timeout: 0
}

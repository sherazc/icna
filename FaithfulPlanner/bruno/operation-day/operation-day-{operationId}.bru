meta {
  name: /operation-day/{operationId}
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/api/company/1/operation-day/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
  timeout: 0
}

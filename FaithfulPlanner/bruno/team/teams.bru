meta {
  name: /teams
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/company/1/teams
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
  timeout: 0
}

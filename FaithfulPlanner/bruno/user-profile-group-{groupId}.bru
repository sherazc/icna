meta {
  name: /user-profile/group/{groupId}
  type: http
  seq: 8
}

get {
  url: {{base_url}}/api/company/1/user-profile/group/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
  timeout: 0
}
